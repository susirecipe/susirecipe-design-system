import React from 'react';
import ComponentTemplate from 'templates/ComponentTemplate';

const Header: React.FC = () => {
  return <ComponentTemplate header="Header" content={<></>} />;
};

export default Header;
